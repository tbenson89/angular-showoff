import { StartsWithFilterTsPipe } from './starts-with-filter.ts.pipe';

describe('StartsWithFilterTsPipe', () => {
  it('create an instance', () => {
    const pipe = new StartsWithFilterTsPipe();
    expect(pipe).toBeTruthy();
  });
});
